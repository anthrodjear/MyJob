/**
 * Create resume page — Server Component wrapper.
 *
 * Renders client-side NewResumePageClient for form interactivity.
 */

import { NewResumePageClient } from "./NewResumePageClient";

export default function NewResumePage() {
  return <NewResumePageClient />;
}
