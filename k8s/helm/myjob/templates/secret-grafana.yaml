{{/*
Grafana admin password is created manually via kubectl.
Create the secret: kubectl create secret generic grafana-admin -n monitoring --from-literal=password=yourpassword
Or use helm --set to inject at deploy time.
*/}}
