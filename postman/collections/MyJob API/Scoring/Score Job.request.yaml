$kind: http-request
name: Score Job
method: POST
url: '{{baseUrl}}/api/v1/scoring/score'
order: 1000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "job_id": "{{jobId}}"
    }
