$kind: http-request
name: Create Cover Letter
method: POST
url: '{{baseUrl}}/api/v1/cover-letters'
order: 3000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "job_id": "{{jobId}}",
      "resume_id": "{{resumeId}}"
    }
