$kind: http-request
name: Complete Setup
method: POST
url: '{{baseUrl}}/api/v1/auth/setup'
order: 2000
auth:
  type: noauth
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "username": "admin",
      "email": "admin@example.com",
      "password": "securepassword"
    }
